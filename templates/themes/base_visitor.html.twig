<!DOCTYPE html>
<html lang="fr">

	<head>
		{# L'encodage des caractères #}
		<meta charset="UTF-8">

		{# Le Minimum de responsive design #}
		<meta name="viewport" content="width=device-width, initial-scale=1">

		{# Le titre de chaque page dans l'onglet #}
		<title>
			{% block title %}{% endblock %}
			- Le site de transfert d'argent DBS</title>

		{# La description de chaque page #}
		<meta
		name="description" content="{% block description %}{% endblock %}">

		{# Les balises utiles pour le SEO #}
		<meta name="robots" content="index, follow">
		<meta name="theme-color" content="#ffffff">
		<meta name="author" content="dwwm">
		<meta
		name="publisher" content="dwwm">

		{# La favicon #}
		<link rel="apple-touch-icon" sizes="180x180" href="{{asset('apple-touch-icon.png')}}">
		<link rel="icon" type="image/png" sizes="32x32" href="{{asset('favicon-32x32.png')}}">
		<link rel="icon" type="image/png" sizes="16x16" href="{{asset('favicon-16x16.png')}}">
		<link rel="manifest" href="{{asset('site.webmanifest')}}">
		<link rel="mask-icon" href="{{asset('safari-pinned-tab.svg')}}" color="#5bbad5">
		<meta
		name="msapplication-TileColor" content="#da532c">

		{# Font family #}
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link
		href="https://fonts.googleapis.com/css2?family=Pacifico&family=Poppins:ital@1&display=swap" rel="stylesheet">

		{# Font awesome #}
		<link
		rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css" integrity="sha512-z3gLpd7yknf1YoNbCzqRKc4qyor8gaKU1qmn+CShxbuBusANI9QpRohGBreCFkKxLhei6S9CQXFEbbKuqLg0DA==" crossorigin="anonymous" referrerpolicy="no-referrer"/>


		{% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
        {% endblock %}

		{# Ma feuille de style #}
		<link rel="stylesheet" href="{{asset('css/app.css')}}">
	</head>

	<body>
		{# La barre de navigation #}
		<nav class="navbar navbar-expand-md";>
			<div class="container-fluid">
				<a class="navbar-brand" href="{{path('visitor_welcome_index')}}">
					<img class="custom-logo" style="max-width: 100px; height: auto;" src="{{ asset('images/logo/logo.jpeg') }}" alt="Logo">
				</a>

				<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
					<span class="navbar-toggler-icon"></span>
				</button>
				<div class="collapse navbar-collapse" id="navbarNav">
					<ul class="navbar-nav">
						<li class="nav-item">
							<a class="nav-link active" aria-current="page" href="{{path('visitor_welcome_index')}}">Accueil</a>
						</li>
						<li class="nav-item">
							<a class="nav-link" href="{{path('visitor_welcome_index')}}">Qui sommes-nous?</a>
						</li>
						
						<li class="nav-item">
							<a class="nav-link" href="{{path('visitor_contact_create')}}">Contact</a>
						</li>
					</ul>
					<ul class="navbar-nav ms-auto">
                    {% if app.user %}
					    {% if is_granted('ROLE_ADMIN') %}
						    <li class="nav-item">
							    <a class="my-2 mx-lg-2" href="{{path('admin_home_index')}}">Espace admin</a>
							</li>
						{% endif %}

					{% if is_granted('ROLE_USER') %}
						    <li class="nav-item">
							    <a class="my-2 mx-lg-2" href="{{path('user_home_index')}}">Espace user</a>
							</li>
						{% endif %}
                        {# Si un utilisateur est connecté #}
                        <li class="nav-item">
                            <a class="my-2 mx-lg-2" href="{{ path('app_logout') }}">Déconnexion</a>
                        </li>
                        {% if 'ROLE_ADMIN' in app.user.roles %}
                            {# Si l'utilisateur est administrateur #}
                            <li class="nav-item">
                                <a class="my-2 mx-lg-2" href="{{ path('visitor_authentification_register') }}" >Inscription</a>
                            </li>
                        {% endif %}
                    {% else %}
                        {# Si aucun utilisateur n'est connecté #}
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('visitor_authentification_login') }}">Connexion</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="{{ path('visitor_authentification_register') }}" >Inscription</a>
                        </li>
                    {% endif %}
                </ul>
				</div>
			</div>
		</nav>
    
		{# Le contenu spécifique à chaque page #} 
		{% block main %}{% endblock %}

		{# Le pieds de page #}
		<footer class="text-center";>
			<p>dbs</p>
		</footer>
	</body>

</html>
