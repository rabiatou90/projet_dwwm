{% extends "themes/base_admin.html.twig" %}

{% block title %}Voire les agents {{user.id }}{% endblock %}

{% block description %}Voire les agents : {{user.id }}{% endblock %}


{% block main %}
    <h1 class="text-center my-3 display-5">User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ user.prenom }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.nom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ user.contact }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ user.pays }}</td>
            </tr>
            
        </tbody>
    </table>

    <a class=mx-2 text-secondary href="{{ path('admin_user_index') }}">Retour à la liste</a>

    {# <a class=mx-2 text-secondary href="{{ path('admin_user_edit', {'id': user.id}) }}"><i class="fas fa-edit"></i></a> #}

    {{ include('pages/admin/user/_delete_form.html.twig') }}
{% endblock %}
